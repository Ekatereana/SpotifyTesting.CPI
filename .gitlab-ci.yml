image: markhobson/maven-chrome:jdk-15

stages:
  - test

variables:
  MAVEN_OPTS: "-Dhost"

cache:
  paths:
    - .m2/repository/
    - target/


test:
  stage: test
  services:
    - selenium/standalone-firefox
  script:
    - mvn test $MAVEN_OPTS=--host=selenium__standalone-chrome

